'use client'
import React from "react";

interface ILoginProps { }

const Loading: React.FunctionComponent<ILoginProps> = (props: ILoginProps) => {

    return (
        <div>

        </div>
    )
}

export default Loading;
