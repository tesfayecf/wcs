'use client'
import React from "react";

interface IRecoverProps { }

const Recover: React.FunctionComponent<IRecoverProps> = (props: IRecoverProps) => {

    return (
        <div id="recover" className="recover">

        </div>
    );
};

export default Recover;
